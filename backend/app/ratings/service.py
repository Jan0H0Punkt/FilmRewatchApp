"""Business-logic layer for the ratings module (DESIGN §5.1).

Validation and the ratings-specific rules live here, calling ``repository.py``
through a stable interface; no HTTP, no raw SQL. Empty stub — logic arrives in M1.
"""

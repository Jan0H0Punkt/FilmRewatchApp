"""Business-logic layer for the rewatch module (DESIGN §5.8).

Assembles the FR-RW-02 per-film payload, invokes the pure algorithm, and persists
the ordered due-list. Stays unaware of the scheduler that triggers it. Empty stub
— arrives in M4.
"""
